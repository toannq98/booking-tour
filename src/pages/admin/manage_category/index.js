import classNames from "classnames/bind";
import style from './manage_category.module.scss';

const cx = classNames.bind(style);

function ManageCategory() {
    return (
        <div className={cx("wrapper")}>
            ManageCategory
        </div>
    );
}

export default ManageCategory;