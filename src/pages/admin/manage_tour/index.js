import classNames from "classnames/bind";
import style from './manage_tour.module.scss';

const cx = classNames.bind(style);

function ManageTour() {
    return (
        <div className={cx("wrapper")}>
            ManageTour
        </div>
    );
}

export default ManageTour;