import classNames from "classnames/bind";
import style from './manage_user_reviews.module.scss';

const cx = classNames.bind(style);

function ManageUserReviews() {
    return (
        <div className={cx("wrapper")}>
            ManageUserReviews
        </div>
    );
}

export default ManageUserReviews;